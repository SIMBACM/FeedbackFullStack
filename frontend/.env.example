# Frontend Environment Variables
# Copy this to .env.local for local development

# Backend API URL - Optional, will be auto-detected if not set
# Development: Auto-detects http://localhost:8080/api
# Production: Auto-detects from current domain or uses this value
# VITE_API_BASE_URL=https://your-backend-service.onrender.com/api

# Backend Port (for development only)
# VITE_BACKEND_PORT=8080

# Optional: Analytics or other frontend-specific configs
# VITE_ANALYTICS_ID=your_analytics_id
